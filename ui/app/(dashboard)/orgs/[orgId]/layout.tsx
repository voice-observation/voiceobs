"use client";

/**
 * Layout for org-scoped routes (agents, test-suites).
 * OrgSwitcher handles redirect when user switches org.
 */
export default function OrgLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
