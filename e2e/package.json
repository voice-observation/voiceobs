{
  "name": "voiceobs-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for voiceobs",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:auth": "playwright test specs/auth.spec.ts",
    "test:org": "playwright test specs/organization.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.0",
    "typescript": "^5.5.0"
  }
}
